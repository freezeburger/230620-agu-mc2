import { ImgOverlayDirective } from './img-overlay.directive';

describe('ImgOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
