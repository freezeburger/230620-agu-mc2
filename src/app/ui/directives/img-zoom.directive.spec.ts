import { ImgZoomDirective } from './img-zoom.directive';

describe('ImgZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgZoomDirective();
    expect(directive).toBeTruthy();
  });
});
